# First tip

When you clone a project from Github, the branches will be hidden, but you can find them with "git branch -a" command, and move to them normally by using the "git checkout <branch>" command.
